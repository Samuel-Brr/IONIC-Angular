import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.cours.max',
  appName: 'ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
